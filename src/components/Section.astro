---
const { title } = Astro.props;
---

<section class="max-w-2xl mx-[auto] mb-16">
    {title && <h2 class="mb-4 font-bold text-3xl leading-4 text-slate-900 dark:text-slate-100">{title}</h2>}
    <slot />
</section>
